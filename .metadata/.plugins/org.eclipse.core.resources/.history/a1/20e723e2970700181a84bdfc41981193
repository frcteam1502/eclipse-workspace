package org.usfirst.frc.team1502.robot;

public class Timing {
	/*
	 * start new thread
	 * set timeout
	 * set interval
	 * clear timeout
	 * clear interval
	 */
	public int startNewThread(Runnable r) {
		return 0;
	}
	public int setTimeout(Runnable r) {
		return 0;
	}
	public int setInterval(Runnable r) {
		return 0;
	}
	public void clearTimeout(Runnable r) {
		
	}
	public void clearInterval(Runnable r) {
		
	}
}
